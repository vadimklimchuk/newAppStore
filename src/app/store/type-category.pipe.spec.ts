import { TypeCategoryPipe } from './type-category.pipe';

describe('TypeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
